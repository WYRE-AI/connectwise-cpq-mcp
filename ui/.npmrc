# No lockfile: @modelcontextprotocol/ext-apps peer-depends on the v1
# @modelcontextprotocol/sdk, and the fleet's v2 migration contract bans that
# package name from any committed lockfile. This package is build-time-only —
# its output (../src/generated/quote-card-html.ts) is committed and reviewed.
package-lock=false
